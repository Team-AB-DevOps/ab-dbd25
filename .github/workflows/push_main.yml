name: Push to Main Branch

on:
  push:
    branches:
      - main

jobs:
  terraform-apply:
    uses: ./.github/workflows/terraform-apply.yml
    secrets:
      HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}
